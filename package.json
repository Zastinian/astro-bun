{
  "workspaces": ["package", "test"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "dev": "bun run --filter '*' dev",
    "preview": "cd test && bun run dist/server/entry.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
